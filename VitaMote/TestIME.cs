// First thing to do, check if we receive anything from the network.
// Then, see if we can get the associated events without a keyboard, and execute actions (a simple text will suffice)

using Android.App;
using Android.InputMethodServices;

namespace VitaMote
{
    //[IntentFilter]
    public class TestIME : InputMethodService
    {

    }
}